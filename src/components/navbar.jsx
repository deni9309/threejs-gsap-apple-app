import { appleImg } from '../utils'

const Navbar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} alt="apple" height={18} width={14} />
      </nav>
    </header>
  )
}

export default Navbar
